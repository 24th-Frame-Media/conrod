[workspace]
resolver = "2"
members = ["crates/conrod-core", "crates/conrod-vision", "crates/conrod-io", "crates/conrod-cli", "spikes/rawprobe", "spikes/detbench"]
# Spikes pull ONNX Runtime and GPU runtimes (hundreds of MB); CI and a plain
# `cargo test` build only the product crates. Run a spike with -p.
default-members = ["crates/conrod-core", "crates/conrod-vision", "crates/conrod-io", "crates/conrod-cli"]

[workspace.package]
version = "0.1.0"
edition = "2021"
license = "AGPL-3.0-only"
repository = "https://github.com/kapsikkum/conrod"

[profile.release]
lto = "thin"
codegen-units = 1
